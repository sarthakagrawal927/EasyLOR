mutation UpdateStudent($input: UpdateStudentInput!) {
	updateStudent(updateStudentInput: $input) {
		user {
			id
			firstName
			lastName
		}
		regNo
		appliedUniversities
		acceptedUniversity
		testScores {
			id
			exam
			score
		}
	}
}
