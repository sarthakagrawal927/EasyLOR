mutation CreateUser($createUserInput: CreateUserInput!) {
    createUser(createUserInput: $createUserInput) {
        email
        firstName
        lastName
        department {
            name
        }
        institution
        contact
        profilePhoto
        userType
        token
    }
}
