overwrite: true
schema: "http://localhost:4000"
documents: "api/graphql/**/*.graphql*"
generates:
    entities/types.graphql.ts:
        plugins:
            - "typescript"
            - "typescript-operations"
            - "typescript-react-apollo"
